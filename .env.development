# 本地开发环境配置(此文件被 .gitignore 忽略,不入库)

# 代理方式:浏览器请求同源 /api/*,由 dev server 转发到隧道 → 无 CORS 问题
VITE_API_BASE=
# 本机后端(5000 被占用,后端固定用 5001)。要回到隧道:换成 pinggy 给出的
# https://xxx-xx-xx-xx-xx.run.pinggy-free.link 即可,其它不用动。
VITE_PROXY_TARGET=https://qtonq-144-48-80-211.run.pinggy-free.link
# 若后端已配好 CORS(允许 http://localhost:5173 + Allow-Credentials),
# 可改用直连方式:VITE_API_BASE=隧道地址,VITE_PROXY_TARGET 留空
